# Database Schema

> **Generated by:** Database Agent
> **Date:** 2026-04-10 (re-verified)
> **Database:** postgres @ localhost:5432

---

## Table: `users`

| Column | Type | Constraints | Default | Description |
|--------|------|-------------|---------|-------------|
| `id` | `UUID` | `PRIMARY KEY` | `uuid_generate_v4()` | Unique user identifier |
| `name` | `VARCHAR(255)` | `NOT NULL` | — | Display name |
| `email` | `VARCHAR(255)` | `UNIQUE`, `NOT NULL` | — | Login email |
| `password_hash` | `VARCHAR(255)` | `NOT NULL` | — | bcrypt-hashed password |
| `created_at` | `TIMESTAMPTZ` | `NOT NULL` | `NOW()` | Record creation time |
| `updated_at` | `TIMESTAMPTZ` | `NOT NULL` | `NOW()` | Last update time |

### Indexes

| Name | Type | Column(s) |
|------|------|-----------|
| `users_pkey` | PRIMARY KEY (btree) | `id` |
| `users_email_key` | UNIQUE (btree) | `email` |
| `idx_users_email` | INDEX (btree) | `email` |

### Seed Data (Development)

| name | email | password (plaintext) |
|------|-------|---------------------|
| Admin | `admin@example.com` | `password123` |
| Alice | `alice@example.com` | — |
| Bob1 | `bob1@example.com` | — |
| Bob | `bob2@example.com` | — |

Passwords are hashed with `bcrypt.hashpw()` + `bcrypt.gensalt()` before insertion.

---

## Notes

- UUID primary keys via `uuid_generate_v4()` (uuid-ossp extension)
- `password_hash` stores bcrypt output (60-character string)
- Unique constraint on `email` prevents duplicate accounts
- `TIMESTAMPTZ` used instead of `TIMESTAMP` for timezone-aware timestamps
- Additional btree index on `email` (`idx_users_email`) for login query performance
